[project]
name = "gascomp-duoke-sync"
version = "0.1.0"
description = "Authorized Duoke catalog sync and Gascomp support knowledge automation"
requires-python = ">=3.11"
dependencies = [
  "playwright>=1.55,<2",
  "scrapling[fetchers]>=0.4.8,<0.5",
]

[tool.uv]
package = false
