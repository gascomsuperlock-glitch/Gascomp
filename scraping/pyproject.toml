[project]
name = "gascomp-duoke-sync"
version = "0.1.0"
description = "Authenticated Duoke product capture and normalized Gascomp catalog import"
requires-python = ">=3.11"
dependencies = [
  "scrapling[fetchers]>=0.4.8,<0.5",
]

[tool.uv]
package = false
